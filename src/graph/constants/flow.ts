import { NOTE } from "./note";

export const FLOW = {
  width: 2.6,
  arrowLength: 16,
  arrowWidth: 12,
  bendRadius: NOTE.radius,
  haloSlots: 16,
  haloRadius: 50,
  anchorPad: 20,
  anchorGap: 14,
} as const;
